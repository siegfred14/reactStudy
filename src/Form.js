import React, { Component } from "react";

class Form extends Component {
  render() {
    return (
      <form>
        <label>Username</label>
        <input type="text" />
      </form>
    );
  }
}

export default Form;
